{
  "rules": {
    ".read": false,
    ".write": false,
    "rooms": {
      "$roomId": {
        ".read": "auth != null",
        "players": {
          "$playerId": {
            ".write": "auth != null && $playerId == auth.uid"
          }
        }
      }
    }
  }
}